/*
 * File: 5-printf.c
 * Auth: AyzCoded
 */
#include <stdio.h>
/**
 * main - Prints "with prooer grammar, but the outcome is
 * a piece of art," followed by a new line.
 *
 * Return: Always 0.
 */
int main(void)
{
  printf("with proper grammar, but the outxome is a piece of art,\n");
  return (0);
}
